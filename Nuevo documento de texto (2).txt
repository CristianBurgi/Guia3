/*
Realiza un programa en donde una clase Persona tenga como atributo nombre, apellido y un
objeto de clase Dni. La clase Dni tendrá como atributos la serie (carácter) y número. Prueba
acceder luego a los atributos del dni de la persona creando objetos y jugando desde el main.
 */
package relacionentreclase1;

public class RelacionEntreClase1 {

    public static void main(String[] args) {
        Dni documento1 = new Dni('A', 28456876); //Constructor con parametros.
        Dni documento2 = new Dni(); //Instancia con el Constructor vacio.
        
        documento2.setCaracter('B'); // Seteo de valor
        documento2.setNumero(42567098); // Seteo de valor.
        
        System.out.println(documento2.getNumero()); // Llamada al metodo get
        System.out.println(documento2.getCaracter()); // LLamada al metodo get
        System.out.println(documento1.toString()); // metodo to String
        System.out.println(documento1); 
        
        Persona gente1 = new Persona("flacundo", "garcia", documento1);
        System.out.println(gente1);
        
        Persona gente2 = new Persona(); //Instanciacion de un nuevo objeto
        gente2.setNombre("Eduardo"); //seteo de valores
        gente2.setApellido("ATR PERRO"); //seteo de valores
        gente2.setDni(documento2); //seteo de valores
        
        System.out.println(gente2); 
    }

}


/*
Realiza un programa en donde una clase Persona tenga como atributo nombre, apellido y un
objeto de clase Dni. La clase Dni tendrá como atributos la serie (carácter) y número. Prueba
acceder luego a los atributos del dni de la persona creando objetos y jugando desde el main.
 */
package relacionentreclase1;

/**
 *
 * @author Criss Candioti
 */
public class Persona {

    private String nombre;
    private String apellido;
    private Dni dni; //Relacion con la clase dni.

    public Persona() {
    }

    public Persona(String nombre, String apellido, Dni dni) {
        this.nombre = nombre;
        this.apellido = apellido;
        this.dni = dni;
    }

    public String getNombre() {
        return nombre;
    }

    public void setNombre(String nombre) {
        this.nombre = nombre;
    }

    public String getApellido() {
        return apellido;
    }

    public void setApellido(String apellido) {
        this.apellido = apellido;
    }

    public Dni getDni() {
        return dni;
    }

    public void setDni(Dni dni) {
        this.dni = dni;
    }

    @Override
    public String toString() {
        System.out.println("");
        return "Persona{" + "nombre=" + nombre + ", apellido=" + apellido + ", dni=" + dni + '}';
    }

}


/*
Realiza un programa en donde una clase Persona tenga como atributo nombre, apellido y un
objeto de clase Dni. La clase Dni tendrá como atributos la serie (carácter) y número. Prueba
acceder luego a los atributos del dni de la persona creando objetos y jugando desde el main.
 */
package relacionentreclase1;

/**
 *
 * @author Criss Candioti
 */
public class Dni {

    private char caracter;
    private int numero;

    public Dni() {
    }

    public Dni(char caracter, int numero) {
        this.caracter = caracter;
        this.numero = numero;
    }

    public char getCaracter() {
        return caracter;
    }

    public void setCaracter(char caracter) {
        this.caracter = caracter;
    }

    public int getNumero() {
        return numero;
    }

    public void setNumero(int numero) {
        this.numero = numero;
    }

    @Override
    public String toString() {
        return "Dni{" + "caracter=" + caracter + ", numero=" + numero + '}';
    }

}
